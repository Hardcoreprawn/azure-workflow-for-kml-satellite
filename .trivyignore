# Temporary exceptions; expired entries remain blocking until reviewed.
# Remove obsolete entries only with fresh scanner evidence and control review.
# All entries must have an exp: date. Review any entry older than 30 days.
#
# AZU-0012 / AZU-0013 are tied to strict network ACL posture for Storage/Key Vault.
# Both dev and prd templates lack deny-by-default ACLs. Owner accepted the current
# pre-customer cost posture on 2026-09-12; review before customer onboarding or
# expiry, whichever comes first. Production freeze and authentication stay intact.
# Private networking and additional paid Defender coverage remain deferred;
# obsolete scanner exceptions are not reinstated by this cost decision. #1500
AZU-0012 # exp:2026-12-12 Storage network ACL - owner-accepted pre-customer risk; #1500; #1362
AZU-0013 # exp:2026-12-12 Key Vault network ACL - owner-accepted pre-customer risk; #1500; #1361
# AZU-0022 removed — Cosmos public network is now env-scoped via cosmos_public_network_access

# AVD-AZU-0057: Modern diagnostics cover blob writes/deletes, not reads or all
# storage services. Equivalence to the scanner's logging requirement is unproven.
# Owner accepted this residual coverage gap for the same pre-customer period.
AVD-AZU-0057 # exp:2026-12-12 logging coverage - owner-accepted pre-customer risk; #1500; #1360

# Container CVEs may only be suppressed when all of these are true:
# - the component is vendored by an upstream runtime and cannot be upgraded here;
# - a fresh no-cache image scan proves the finding remains after upstream refresh;
# - the entry links a tracking issue and expires within 30 days.
# Actionable first-party or directly upgradeable findings remain blocking.
